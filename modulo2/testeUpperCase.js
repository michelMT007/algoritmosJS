const { stringify } = require("querystring");

var nomesDosPresentes = ["Talissa", "Raissa", "Joao", "Peter", "Paul", "Brian", "Suse", "Ubuntu", "Lars", "Denni", "Robert"];

var p=nomesDosPresentes[0].toUpperCase();

console.log(p.toUpperCase());